# T011 - Add migration, conformance, security, concurrency, and end-to-end coverage

**Depends on:** T006, T007, T008, T009, T010

## Scope

Prove the complete source specification with deterministic cross-platform
fixtures and regression/performance checks.

## Acceptance criteria

- [ ] All 14 end-to-end scenarios in `memory-okf-native.md` have automated coverage.
- [ ] OKF 0.2 permissive conformance and warned 0.1 fallbacks have fixtures.
- [ ] Unknown fields, raw frontmatter, body, links, sources, verification, and extensions round-trip.
- [ ] Historical DB upgrades, rollback, restart, and idempotent reopen pass.
- [ ] CAS conflicts and concurrent WAL readers/writers pass without long write locks.
- [ ] Hostile YAML, oversized documents, unsafe paths/archives, FTS payloads, link bombs, and computation metadata are safe.
- [ ] Hook latency/output and graph fan-out obey deterministic budgets.
- [ ] Legacy CLI, MCP, search, Dream, and gateway suites remain green.
- [ ] `cargo fmt --all --check`, clippy with warnings denied, workspace tests, and workspace build pass.

## Touch surface

- workspace integration tests
- OKF and migration fixtures
- performance/regression harnesses
